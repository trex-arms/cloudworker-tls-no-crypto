# Example CLI usage:

```sh
cloudworker file_to_run.js
	-r # watch mode
	-p 80 # http port
	--tls-key ../cert-key.pem
	--tls-cert ../cert.pem
	--https-port 4000 # https port
```

# license

MIT
